[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "modern-python-guidance"
version = "0.6.0"
description = "Version-aware BAD/GOOD pattern guides that help AI coding agents generate modern Python"
readme = "README.md"
license = "Apache-2.0 OR MIT"
requires-python = ">=3.11"
authors = [
    { name = "Iori Yoshida", email = "i.yoshida@raksul.com" },
]
keywords = ["python", "modernization", "ai", "coding-agent", "linter", "guidance"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "License :: OSI Approved :: MIT License",
    # Linux is what CI runs; macOS is where this is developed. Windows is
    # deliberately absent: `mpg setup` creates symlinks, which need Developer
    # Mode or an elevated prompt there, and nothing has verified it. Claiming
    # the platform in metadata while README calls it untested would be the same
    # kind of false promise the Typing classifier was making without py.typed.
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Quality Assurance",
    "Typing :: Typed",
]
dependencies = [
    "packaging>=23.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
]

[project.scripts]
modern-python-guidance = "modern_python_guidance.cli:main"
mpg = "modern_python_guidance.cli:main"

[project.urls]
Homepage = "https://github.com/yottayoshida/modern-python-guidance"
Repository = "https://github.com/yottayoshida/modern-python-guidance"
Issues = "https://github.com/yottayoshida/modern-python-guidance/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/modern_python_guidance"]
only-include = ["src/modern_python_guidance", "skills", "rules"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
"skills" = "modern_python_guidance/skills"
"rules" = "modern_python_guidance/rules"

[tool.ruff]
target-version = "py311"
line-length = 99
# Benchmark inputs, not source: they carry outdated patterns on purpose, and even
# the "modern" variants trip rules this project's guides say nothing about. There
# is no subset of rules that makes them clean, so they are not linted at all.
# Everything else under bench/ — the runner and scorers — stays in scope.
exclude = ["bench/fixtures"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "FURB", "B", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
"bench/*.py" = ["SIM102", "SIM110"]
"src/modern_python_guidance/setup_cmd.py" = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["modern_python_guidance"]
branch = true

[tool.coverage.report]
show_missing = true
skip_empty = true
fail_under = 92
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
    "if TYPE_CHECKING:",
]
